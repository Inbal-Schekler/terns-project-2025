# Chicks Pipeline

> **Work in progress.** This pipeline detects and counts dead chicks in the tern colony.

The first steps (scan control and video-to-image conversion) are shared — see the root `RealCoordinatesCalculator/` and `ConvertVideoToImage/` folders.
